import React from 'react';
import './Spinner.scss';

const Spinner = () => (
	<div className="loader">
		<div className="circle" />
		<div className="circle" />
		<div className="circle" />
	</div>
);

export default Spinner;
